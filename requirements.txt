# Direct runtime dependencies for the football_ai package and shipped scripts.
# Keep this file curated for open-source installs; do not freeze a local
# environment with transitive packages such as IPython, Jupyter, or debug tools.

# Core data and ML stack
numpy>=1.24,<3
pandas>=2.0,<3
scikit-learn>=1.4,<2
scipy>=1.11,<2

# Football analytics and HDF5 I/O
socceraction>=1.5,<2
tables>=3.9,<4

# Configs and progress bars
PyYAML>=6.0,<7
tqdm>=4.66,<5

# XGBoost training and Bayesian tuning entrypoints
xgboost>=2.0,<4
optuna>=3.0,<5

# Plotting and EDA scripts
matplotlib>=3.5,<4
seaborn>=0.13,<1
